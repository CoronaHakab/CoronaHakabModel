from typing import List, Container

import numpy as np

class Cluster:
    def __init__(self, indices: List[int]):
        self.indices = sorted(indices)
        self.arr =

class ClusteredSparseMatrix:
    def __init__(self, clusters: List[Container[int]]):